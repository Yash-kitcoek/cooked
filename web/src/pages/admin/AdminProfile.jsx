import React from 'react'; import { ShieldCheck, LogOut } from 'lucide-react'; import { useAuth } from '../../context/AuthContext'; import { useNavigate } from 'react-router-dom';
export function AdminProfile(){const {session,logout}=useAuth();const navigate=useNavigate();return <div className="admin-page"><section className="admin-hero page-hero"><div><div className="eyebrow-neon">ACCOUNT</div><h1>Admin profile</h1><p>Your administrative identity and access information.</p></div></section><section className="profile-card"><div className="profile-avatar"><ShieldCheck size={34}/></div><div className="profile-details"><div className="panel-eyebrow">IDENTITY</div><h2>{session?.username||session?.email||'Administrator'}</h2><p>{session?.email||'No email returned by session.'}</p><div className="profile-tags"><span>ADMIN</span><span>AUTHORIZED</span></div></div></section><section className="admin-panel"><div className="panel-eyebrow">SESSION</div><h2>Administrative access</h2><p className="detail-copy">This account is protected by the existing role-based route guard. Signing out clears the current client session.</p><button className="admin-button danger" onClick={()=>{logout();navigate('/login')}}><LogOut size={15}/> Sign out</button></section></div>}
